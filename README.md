# Echo in the Forest

A Next.js 15 App Router site for Echo in the Forest.

## Run Locally

```bash
npm install
npm run dev
```

## Add an Article

Add a new markdown file in `content/articles/` with front matter for title, slug, date, subtitle, excerpt, order, and related posts.
